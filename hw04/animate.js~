pic = document.getElementById("svgid");

document.getElementByTagName("circle");

var drawC function(){
    var radius = 0;
    var grow = false;

    createElementNS(c1, circle);
    c1.setAttribute("cx", pic.width/2);
    c1.setAttribute("cy", pic.height/2);
    c1.setAttribute("r", radius);
    c1.setAttribute("stroke","black");
    c1.setAttribute("fill","red");
    
    var animate function(){

	pic
	
	if 
	
	if (grow)
	    radius++;
	else
	    radius--;
	
    }
    setInterval(animate, 1000);   
}
